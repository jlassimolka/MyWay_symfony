{% extends 'baseBack.html.twig' %}

{% block title %}New Reponse{% endblock %}

{% block tableau %}

    {{ include('reponse/_form.html.twig') }}
    <a class="btn btn-primary" style="width:300px; margin:1px; background-color:#8DD3BB; margin: 0 auto; display: block;" type="submit" href="{{ path('app_reponse_index') }}">back to list</a>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Repoonse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reponse in reponses %}
            <tr>
                <td>{{ reponse.id }}</td>
                <td>{{ reponse.reponse }}</td>
                <td>
                    <a class="btn btn-warning" href="{{ path('app_reponse_show', {'id': reponse.id}) }}">show</a>
                    <a class="btn btn-danger" href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    

{% endblock %}
