{% extends 'baseBack.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block tableau %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Reponse</th>
                <td>{{ reponse.reponse }}</td>
            </tr>
            <tr>
                <th>Reclamation</th>
                <td>{{ reponse.message.titre }}</td>
            </tr>
        </tbody>
    </table>

    <div class="flex-grow-1" >
    <a class="btn btn-success" href="{{ path('app_reponse_index') }}">back to list</a>
    <a class="btn btn-warning" href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>
    {{ include('reponse/_delete_form.html.twig') }}
    </div>
{% endblock %}
