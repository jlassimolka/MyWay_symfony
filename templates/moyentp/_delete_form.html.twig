<form method="post" action="{{ path('app_moyentp_delete', {'id': moyentp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moyentp.id) }}">
    <button class="btn">Delete</button>
</form>
