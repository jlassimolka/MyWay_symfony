<form method="post" action="{{ path('app_guide_delete', {'id': guide.id}) }}" onsubmit="return confirm('Supprimer ce guide definitivement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide.id) }}">
<button class="btn btn-danger">{{ button_label|default('Supprimer ce guide ') }}</button>
</form>
